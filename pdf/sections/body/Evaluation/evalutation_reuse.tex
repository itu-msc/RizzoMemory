\todo{The goal this section to show (using a Rizzo program) that we can correctly reuse the memory for values inside signals. OR that reuse does work.}

\todo{Let's do something like a \texttt{list\_map} \dots}
