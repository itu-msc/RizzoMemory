\section{Automatically reference counting Rizzo}\label{section:refcount}

\todo{Quick refresh of assumptions from \textit{Reference Counting section}}

The algorithms by \citeauthor{ullrich_counting_2021}~\cite{ullrich_counting_2021} are defined over the calculi $\lambda_{pure}$ and $\lambda_{RC}$, where the $\lambda_{RC}$ extends $\lambda_{pure}$ with reference counting instructions Figure~\ref{figure:refcount_IR} shows the IR used in our compiler, where Rizzo-specific extensions are highlighted in green.  

In this IR, $\texttt{pap}\; c \; \bar p$ denotes partial application of a global constant $c$, $\texttt{ctor}_i \; \bar p$ constructs a value of some erased type with tag $i$, and $\texttt{proj}_i \; x$ projects the $i\text{-th}$ field from constructor value $x$. Fully applied calls and partial applications may only happen on global constants and variable application is defined separately for only a single argument.

\begin{figure}[h]
	\[
	\begin{aligned}
		w, x, y, z&\in Var\\
		c &\in Const \\
		l &\in \textcolor{greenadditions}{Literal \qquad ::= \mathbb{Z} \mid \text{true} \mid \text{false} \mid \text{never} \mid string}\\
		p &\in \textcolor{greenadditions}{Primitive \qquad ::= x\mid l}\\
		e &\in Expr \qquad ::= c\,\textcolor{greenadditions}{\bar p} \mid \mathbf{pap}\; c\,\textcolor{greenadditions}{\bar p} \mid x\; p \mid \mathbf{ctor}_i\,\textcolor{greenadditions}{\bar p} \mid \mathbf{proj}_i\; x 
		\mid \textcolor{greenadditions}{\texttt{signal} \; p \; p} \\
		F &\in FnBody \; ::= \mathbf{ret}\; \textcolor{greenadditions}{p} \mid \mathbf{let}\; x = e;\; F \mid \mathbf{case}\; x\; \mathbf{of}\; \textcolor{greenadditions}{\overline{\langle \mathbb{Z}, F \rangle}} \\
		f &\in Fn \qquad ::= \lambda\,\bar y.\; F \\
		\delta &\in Program \; = Const \to Fn \mid \textcolor{greenadditions}{Const \to FnBody}
	\end{aligned}
	\]
	\caption{The reference counting IR. Our modifications are marked in green.}
	\label{figure:refcount_IR}
\end{figure}

Observe that the IR is defined in A-normal form~\cite{flanagan_essence_1993}, where expressions are non-recursive and are only allowed on the right-hand-side of let-bindings. 
Our representation keeps this shape but introduce \textit{Primitives} such that variables and literals (i.e.~integers, booleans, strings, and \texttt{never}) can appear directly in returns and as arguments in function/constructor application. This makes translation easier and avoids bindings such as $\texttt{let} \; x = 20; \; \texttt{ret} \; x$ when $\texttt{ret}\;20$ is enough.
We additionally add a specialised $\texttt{signal}$ constructor that can be used in places where $\texttt{ctor}_i$ is expected. This allows runtime to know the fresh memory cell should be stored in the signal heap (see Section~\ref{section:frp:signal_heap}). Finally, we added top level values represented as mappings from constants to $FnBody$.~\todo{is it worth describing what the $FnBody$ means? Why is there?}

\subsection{Transformations}\label{section:refcount:transforms}

The strategy for automatically inserting reference counting is to first transform Rizzo syntax trees into a shape that satisfies the invariants listed in Section~\ref{sec:background:beans:assumptions}, then convert these to the IR, and only then running the algorithms $R$, $collect_{\mathbb{O}}$, and $C$~\cite{ullrich_counting_2021}.

In order to get Rizzo programs into the correct shape, the compiler performs a series of transformations as shown in Figure~\ref{figure:refcount:transformations}.

\begin{figure}[h]
	\centering
	\includegraphics[width=\textwidth]{flowchart_transformations.png}
	\caption{\todo{Transformations}}
	\label{figure:refcount:transformations}
\end{figure}

Although, these transformations are standard, we'll 
\todo{They all get us closer to the invariants with the exception of consec lambda elim}

\todo{Here comes a pretty little flow chart showing how we chain these transformatiosn together, and a little explanation of why/what in relation the assumptions}

\subsection{Counting Rizzo's beans}\label{section:refcount:beans}

\todo{Walk through the 3 steps and explain: Purpose and how have we adapted it for Rizzo?}



\todo{Our adaptation of the immutable bean algorithm. Our modifications. How do we insert these refcount-instructions}

\todo{How do we capture/model the Rizzo constructors, i.e. Delay, Later, Signal, \dots}
