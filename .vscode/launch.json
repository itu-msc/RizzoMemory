{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "OCaml earlybird (experimental)",
      "type": "ocaml.earlybird",
      "request": "launch",
      "program": "${workspaceFolder}/_build/default/src/bin/main.bc",
      "preLaunchTask": "rizzo: build"
    },
    {
      "name": "Run Rizzo Extension (repo root)",
      "type": "extensionHost",
      "request": "launch",
      "runtimeExecutable": "${execPath}",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}/vscode-rizzo-lsp",
        "--folder-uri=${workspaceFolder}",
        "--disable-extensions"
      ],
      "outFiles": ["${workspaceFolder}/vscode-rizzo-lsp/out/**/*.js"],
      "preLaunchTask": "rizzo: prepare extension host"
    }
  ]
}
