let greeting = "Hello" + ", " + "world!"

fun describeText s =
    match s with
    // | "" -> "The string is empty."
    // | "H" :: rest -> "Starts with H, tail = " + rest
    | first :: _rest -> "Starts with " + first

fun entry x =
    let summary = describeText greeting in
    if x == 0 then
        summary
    else
        "Input was not zero."

@effectful
fun _const x : 'a -> Signal 'a = x :: never

fun appendSignalToNeverAndAdd1 s a = _const (a |> s)

fun is0 x = x == 0
