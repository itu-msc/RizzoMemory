# 2026-03-06 

## 'Reference counting with frame limited reuse'

- In order to implement their ideas we would have to re-do our code for the automatic reference counting.
  - It may be that we only to create something similar to our `pure_to_rc.ml`, that translates to some form that is easier to apply the "frame limited reuse" ideas to.
  - Still requires us to come up with (implement) an algorithm for their analysis rules.
- They suggest using type information in the drop-reuse analysis, so such instructions could be inserted without requiring being inside of a match (case) expression. 
  - We may already be able to apply this optimisation in our current form.
- ...
- We established that actual reuse of signals in signal combinators is limited (requires specific conditions - no recursing on tail, and must immediately create a new signal) - unlikely to be a big optimisation for signals but for the rest of Rizzo, it could be good.

